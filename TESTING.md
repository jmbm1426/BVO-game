# الاختبارات و Playwright 🎭

هذا الملف يشرح كيف تشغّل اللعبة محلياً، كيف تشغّل الاختبارات، وكيف يتحكّم
`Claude` بالمتصفح مباشرة عبر `Playwright MCP`.

## التجهيز لمرّة واحدة

```bash
npm install
npm run setup:browsers   # ينزّل متصفح Chromium الخاص بـ Playwright
```

> في البيئات التي يوجد فيها متصفح مثبّت مسبقاً (مثل حاويات `CI`) تُكتشف نسخته
> تلقائياً ولا حاجة لتشغيل `setup:browsers`.

## تشغيل اللعبة محلياً

```bash
npm start            # ثم افتح http://localhost:4173/
```

سيرفر ثابت صغير بدون أي مكتبات خارجية (`scripts/serve.js`).

## تشغيل الاختبارات

```bash
npm test             # كل الاختبارات (جوال + سطح مكتب)
npm run test:headed  # مع إظهار المتصفح
npm run test:ui      # الواجهة التفاعلية لـ Playwright
npm run test:report  # فتح تقرير آخر تشغيل
```

الاختبارات تشغّل السيرفر تلقائياً، فلا حاجة لتشغيل `npm start` قبلها.

### الاختبارات الموجودة

`tests/smoke.spec.js` — اختبارات دخان أساسية:

| الاختبار | ما يتحقّق منه |
| --- | --- |
| تحويل الصفحة الرئيسية | `index.html` ينقل إلى `BVO.html` |
| شاشة الدخول | العنوان، الشعار، وأزرار الدخول الثلاثة |
| الدخول كضيف | الانتقال من `#s-login` إلى `#s-main` |
| اختيار اللغة | فتح وإغلاق `#lang-overlay` |
| نظافة الـ console | لا أخطاء `JavaScript` عند التحميل |

تعمل هذه الاختبارات بدون إنترنت: عند تعذّر الوصول إلى `Firebase` يرجع
`bvoGuestLogin()` إلى `doLogin()` مباشرة.

### لقطات شاشة سريعة

```bash
npm run screenshot
```

يفتح اللعبة، يأخذ لقطة لشاشة الدخول، يسجّل دخولاً كضيف، ويأخذ لقطة للقائمة
الرئيسية — بمقاس الجوال وبمقاس سطح المكتب. تُحفظ في
`test-results/screenshots/`.

هذا الأمر **لا يحتاج `MCP`**، لذلك يعمل في أي مكان يستطيع تنفيذ أوامر: طرفيتك،
أو `Claude Code`، أو `Cowork`.

### مشروعان (projects)

- `mobile` — بمقاسات `Pixel 7` (الاستخدام الأساسي للعبة)
- `desktop` — `1280×800`

لتشغيل واحد فقط: `npx playwright test --project=mobile`

## ربط Claude Code بالمتصفح (Playwright MCP)

> هذا القسم يخصّ `Claude Code` في الطرفية. لتطبيق `Claude` على الحاسب و`Cowork`
> انظر القسم التالي — الملفّان مختلفان تماماً.

`.mcp.json` يسجّل سيرفر `playwright` لدى `Claude Code`، فيصبح `Claude` قادراً على
فتح اللعبة والنقر والكتابة وأخذ لقطات شاشة وقراءة أخطاء الـ console بنفسه.

**للتفعيل:** أعد تشغيل `Claude Code` في هذا المجلد ووافق على السيرفر عند السؤال.
للتأكد: اكتب `/mcp` — يجب أن يظهر `playwright` بحالة `connected`.

بعد ذلك يمكنك أن تطلب مثلاً:

> شغّل اللعبة، سجّل دخول كضيف، وأعطني لقطة شاشة للقائمة الرئيسية

أدوات متاحة لـ `Claude` (26 أداة) منها: `browser_navigate`، `browser_click`،
`browser_type`، `browser_snapshot`، `browser_take_screenshot`،
`browser_console_messages`، `browser_network_requests`، `browser_evaluate`.

### متغيّرات التحكّم

| المتغيّر | الأثر |
| --- | --- |
| `PWMCP_HEADED=1` | يُظهر المتصفح بدل تشغيله مخفياً |
| `PWMCP_SANDBOX=1` | يُبقي صندوق حماية Chromium مفعّلاً حتى مع `root` |
| `PWMCP_NO_SANDBOX=1` | يعطّل صندوق الحماية يدوياً |
| `PLAYWRIGHT_CHROMIUM_PATH` | يفرض مسار متصفح معيّن |
| `PORT` | منفذ السيرفر المحلي (الافتراضي `4173`) |

> صندوق حماية `Chromium` يبقى مفعّلاً على جهازك، ويُعطَّل تلقائياً فقط عند
> التشغيل بصلاحية `root` (الحاويات و `CI`) لأنه لا يعمل هناك.

## ربط تطبيق Claude على الحاسب (و Cowork)

### أولاً: ملفّان مختلفان لا يُخلط بينهما

| الملف | مَن يقرأه |
| --- | --- |
| `.mcp.json` (داخل المستودع) | `Claude Code` في الطرفية فقط |
| `claude_desktop_config.json` (في إعدادات النظام) | تطبيق `Claude` على الحاسب |

تطبيق `Claude` **لا يقرأ** `.mcp.json`، ولهذا لا يظهر `Playwright` فيه قبل
تنفيذ الخطوة التالية.

### التنفيذ

أغلق تطبيق `Claude` **إغلاقاً كاملاً** أولاً (انظر التحذير أدناه)، ثم:

```bash
npm run mcp:install-desktop
```

ولمعاينة ما سيُكتب دون أي تغيير:

```bash
node scripts/install-desktop-mcp.js --dry-run
```

السكربت يقوم بكل شيء: يتحقّق أن التطبيق مغلق، ويجد ملف الإعدادات الصحيح، ويأخذ
نسخة احتياطية، ويدمج إعداد `Playwright` **دون حذف** إعداداتك أو سيرفراتك
الأخرى، ثم يطبع الخطوة التالية.

خيارات إضافية: `--force` (التنفيذ مع التطبيق مشتغلاً)، `--latest` (أحدث إصدار
بدل المثبّت)، `--download-browser` (تنزيل متصفح `Playwright` بدل استخدام
`Chrome`/`Edge`)، `--config <path>` (مسار إعدادات صريح).

### الناتج على Windows

```json
{
  "mcpServers": {
    "playwright": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@playwright/mcp@0.0.81",
               "--output-dir", "C:\\Users\\YOU\\BVO-game\\test-results\\mcp",
               "--browser", "chrome"]
    }
  }
}
```

ثلاثة تفاصيل مقصودة:

- **`cmd /c`** — `npx` وحده لا يعمل في هذا الملف على `Windows`.
- **`--browser chrome`** — يستعمل المتصفح الموجود على جهازك (`Chrome`، وإلا
  `Edge` الموجود دائماً على `Windows`). يوفّر تنزيل ~150MB، ويتجنّب تعارضاً
  حقيقياً: `@playwright/mcp` يعتمد نسخة `playwright` مختلفة عن نسخة المستودع،
  ولكلٍّ رقم إصدار متصفح خاص. وإن لم يوجد أيّ متصفح، يُنزّل السكربت المتصفح
  بالنسخة الصحيحة للسيرفر لا لنسخة المستودع.
- **بلا `--headless`** — المتصفح يظهر أمامك، وهذا هو المطلوب في الاستخدام اليدوي.

### ⚠️ مصيدتان في Windows تُفسدان الإعداد بصمت

**١. يجب إغلاق التطبيق من شريط المهام، لا بإغلاق النافذة.**
إغلاق النافذة يتركه يعمل في الخلفية، وأي تعديل على ملف الإعدادات وهو يعمل
**يُمحى بصمت**. الإغلاق الصحيح: زر أيمن على أيقونة `Claude` في شريط المهام (قد
تكون مخفية تحت السهم `^`) ثم `Quit`. السكربت يفحص هذا ويرفض العمل قبله.

**٢. نسخة المتجر (`MSIX`) تقرأ ملفاً مختلفاً عن الذي يفتحه زرّ `Edit Config`.**

| المسار | الحالة |
| --- | --- |
| `%APPDATA%\Claude\claude_desktop_config.json` | يفتحه زرّ `Edit Config` |
| `%LOCALAPPDATA%\Packages\Claude_*\LocalCache\Roaming\Claude\claude_desktop_config.json` | تقرأه نسخة `MSIX` فعلاً |

هذا سبب شكوى شائعة: «عدّلت الملف ولا شيء يحدث». السكربت يكتب في كل مسار موجود،
فيغطّي نوعي التثبيت.

### التحقّق

1. افتح تطبيق `Claude` من جديد.
2. في **المحادثة العادية**: يجب أن يظهر `playwright` في الأدوات. جرّب:
   «افتح `example.com` وخذ لقطة شاشة». هذه الخطوة مؤكّدة.
3. في **Cowork**: جرّب الطلب نفسه.

### ⚠️ حدود Cowork — اقرأ هذا

تشغيل سيرفرات `MCP` المحلية داخل `Cowork` **ليس مضموناً**. `Cowork` يعمل في
بيئة معزولة، ووثائق `Anthropic` تقول إن سيرفرات `MCP` المحلية غير متاحة فيه.
وهناك تقرير خطأ على `Windows` تحديداً
([claude-code#42453](https://github.com/anthropics/claude-code/issues/42453))
تظهر فيه الأدوات في القائمة لكنها ترجع:

```
This tool has been disabled in your connector settings
```

وقد أُغلق التقرير بصفة `closed as not planned`.

**إن حدث ذلك فليس خطأً في إعدادك.** والبديل جاهز ويعطي نفس الفائدة:

> اطلب من `Cowork`: «شغّل `npm run screenshot` واعرض الصور»
> أو «شغّل `npm test` وأخبرني ما فشل».

`Cowork` يستطيع تنفيذ الأوامر وقراءة الملفات، فيحصل على اللقطات ونتائج
الاختبارات بلا `MCP` إطلاقاً — وهذا هو المسار الأضمن.

### خيار أخير إن لزم MCP داخل Cowork (غير منصوح به)

`Cowork` يقبل `Custom Connector` الذي يتّصل من سحابة `Anthropic`، وهذا يعني
تشغيل السيرفر بوضع `HTTP` (`npx @playwright/mcp --port 8931`) ثم فتح نفق
(`Cloudflare Tunnel` أو `ngrok`) لإعطائه رابط `HTTPS` عاماً، ثم إضافته في
`Customise → Connectors → Add custom connector`.

> 🔒 **تحذير:** هذا الرابط العام يمنح **أي شخص يعرفه** تحكّماً كاملاً بمتصفح
> على حاسبك، و`Playwright MCP` في وضع `HTTP` بلا توثيق افتراضياً. كما أن رابط
> النفق يتغيّر فيحتاج تحديث الإعداد في كل مرّة. لم يُنفَّذ هنا لهذا السبب.

## الملفات المضافة

```
.mcp.json                  تسجيل سيرفر Playwright MCP لدى Claude
playwright.config.js       إعداد الاختبارات (مشروعان + سيرفر تلقائي)
tests/smoke.spec.js        اختبارات الدخان
scripts/serve.js           سيرفر ثابت بدون مكتبات
scripts/chromium-path.js   تحديد مسار المتصفح حسب البيئة
scripts/playwright-mcp.js  مشغّل سيرفر MCP لـ Claude Code
scripts/install-desktop-mcp.js  ربط Playwright بتطبيق Claude على الحاسب
scripts/screenshot.js      لقطات شاشة للعبة (بلا MCP)
```

`copyweb.js` يعتمد قائمة ملفات محدّدة، لذلك لا تدخل ملفات الاختبار في بناء
`Capacitor` للأندرويد.
